# VERSION: 6e2a3b6
# THIS FILE IS AUTOGENERATED - DO NOT EDIT
# Source: https://github.com/Software-Automation-Holdings-LLC/developer-common/blob/bdd0b8d10af630209ceece2507cb15dc1ae9dd0b/github-workflow-templates/auto-rebase-cursor.yml

name: Auto-rebase Cursor PRs

on:
  # Run on schedule to check for conflicted PRs
  schedule:
    - cron: "0 */4 * * *" # Every 4 hours

  # Run when base branch is updated (may cause conflicts)
  push:
    branches:
      - main
      - master

  # Allow manual trigger
  workflow_dispatch:

# Restrict default permissions - expanded at job level
permissions: {}

# Only allow one rebase run at a time per repo
concurrency:
  group: auto-rebase-${{ github.repository }}
  cancel-in-progress: false # Don't cancel in-progress rebases

jobs:
  rebase:
    permissions:
      contents: write
      pull-requests: write
    uses: Software-Automation-Holdings-LLC/developer-common/.github/workflows/auto-rebase-cursor.yml@bdd0b8d10af630209ceece2507cb15dc1ae9dd0b
    secrets:
      DEV_BOT_APP_ID: ${{ secrets.DEV_BOT_APP_ID }}
      DEV_BOT_PRIVATE_KEY: ${{ secrets.DEV_BOT_PRIVATE_KEY }}
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
