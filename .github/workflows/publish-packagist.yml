# VERSION: 90bbb71
# THIS FILE IS AUTOGENERATED - DO NOT EDIT
# Source: https://github.com/Software-Automation-Holdings-LLC/developer-common/blob/8d1fc342aa011694119f8ed188fe04e1b4790d56/github-workflow-templates/publish-packagist.yml

# NOTE: This workflow should only be synced to WordPress plugins and themes.
# Filter criteria for sync tool:
#   - Repository must have composer.json
#   - composer.json must have "type": "wordpress-plugin" or "wordpress-theme"

name: Publish to Packagist

on:
  push:
    tags:
      - "v*" # Triggers on version tags like v1.0.0, v1.2.3, etc.
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (e.g., 1.0.0 - no 'v' prefix needed)"
        required: false
        type: string

# Restrict default permissions - expanded at job level
permissions: {}

jobs:
  publish:
    name: Publish to Packagist
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Generate Bot Token
        id: bot-token
        uses: actions/create-github-app-token@29824e69f54612133e76f7eaac726eef6c875baf # v2
        with:
          app-id: ${{ secrets.DEV_BOT_APP_ID }}
          private-key: ${{ secrets.DEV_BOT_PRIVATE_KEY }}
          owner: Software-Automation-Holdings-LLC

      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          fetch-depth: 0
          token: ${{ steps.bot-token.outputs.token }}

      - name: Get version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ] && [ -n "${{ github.event.inputs.version }}" ]; then
            VERSION="${{ github.event.inputs.version }}"
            # Strip v prefix if provided
            VERSION=${VERSION#v}
          elif [ "${{ github.event_name }}" == "push" ] && [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
            # Strip v prefix from tag
            VERSION=${VERSION#v}
          else
            # Extract version from composer.json
            VERSION=$(jq -r '.version' composer.json)
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Checkout developer-common scripts
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          repository: Software-Automation-Holdings-LLC/developer-common
          path: .developer-common
          sparse-checkout: scripts
          token: ${{ steps.bot-token.outputs.token }}

      - name: Update version in files (local only)
        run: |
          chmod +x .developer-common/scripts/update-plugin-version.sh
          .developer-common/scripts/update-plugin-version.sh "${{ steps.get_version.outputs.version }}"
          echo "Updated version to ${{ steps.get_version.outputs.version }} in local files"

      - name: Get package name
        id: get_package_name
        run: |
          PACKAGE_NAME=$(jq -r '.name' composer.json)
          echo "package_name=$PACKAGE_NAME" >> $GITHUB_OUTPUT
          echo "Package name: $PACKAGE_NAME"

      - name: Setup Python
        uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548 # v6
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: pip install requests

      - name: Create plugin ZIP
        id: create_zip
        run: |
          chmod +x .developer-common/scripts/create-plugin-zip.sh
          ZIP_PATH=$(.developer-common/scripts/create-plugin-zip.sh)
          echo "zip_path=$ZIP_PATH" >> $GITHUB_OUTPUT
          echo "Created ZIP: $ZIP_PATH"

      - name: Upload to Packagist
        env:
          PACKAGIST_API_KEY: ${{ secrets.PACKAGIST_API_KEY }}
          PACKAGIST_API_SECRET: ${{ secrets.PACKAGIST_API_SECRET }}
        run: |
          python .developer-common/scripts/upload-to-packagist.py \
            --api-key "$PACKAGIST_API_KEY" \
            --api-secret "$PACKAGIST_API_SECRET" \
            --package-name "${{ steps.get_package_name.outputs.package_name }}" \
            --zip-path "${{ steps.create_zip.outputs.zip_path }}"

      - name: Verify package
        run: |
          echo "✅ Successfully published version ${{ steps.get_version.outputs.version }}"
          echo "Package: ${{ steps.get_package_name.outputs.package_name }}"
          echo "Check at: https://packagist.com/orgs/software-automation/packages/${{ steps.get_package_name.outputs.package_name }}"
